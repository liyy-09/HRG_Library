<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hrg.dim">

    <application
        android:name="debug.DIMApplication"
        android:allowBackup="true"
        android:icon="@mipmap/dim_ic_launcher"
        android:label="@string/dim_app_name"
        tools:replace="android:label"
        android:roundIcon="@mipmap/dim_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/dim_AppTheme">
        <activity android:name=".activity.DIMMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>